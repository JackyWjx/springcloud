server:
  port: 80
spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      # consul注册中心地址
      host: 116.62.162.85
      port: 8500
      discovery:
#        hostname: 127.0.0.1
        service-name: ${spring.application.name}